const a = 10;

const add = (param1, param2) => param1 + param2

// this function will replace the object of the module

// module.exports = a;

// module.exports = add;
module.exports = {
    a, add
};

// console.log(module)